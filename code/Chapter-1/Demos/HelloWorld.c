#include <stdio.h>	//包含标准库信息 

int main(void)	//程序运行入口，不接受参数（可以留空也可以用void显式说明） 
{				//函数定义域标记-开始 
	printf("HelloWorld!\n");	//调用库函数printf显示字符序列（打印双引号中的字符串，/n为换行符） 
	
	return 0;	//函数执行到此结束 
}				//函数定义域标记-结束
